apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: duckview
resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  # - secret.yaml   # create from secret.example.yaml (or use ExternalSecrets / SealedSecrets)
