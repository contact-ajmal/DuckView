apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: duckview
resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - apps-rbac.yaml   # data apps as pods: service account, role, NetworkPolicy
  # - secret.yaml   # create from secret.example.yaml (or use ExternalSecrets / SealedSecrets)
