apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: duckview-data
  namespace: duckview
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 200Gi
  # storageClassName: fast-ssd
